display_information:
  name: 入退室通知bot
features:
  bot_user:
    display_name: notifier_bot
    always_online: false
oauth_config:
  scopes:
    bot:
      - chat:write
      - incoming-webhook
settings:
  org_deploy_enabled: false
  socket_mode_enabled: false
  token_rotation_enabled: false
